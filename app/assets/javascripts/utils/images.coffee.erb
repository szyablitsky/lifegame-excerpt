<%
images = {}
Dir.chdir("#{Rails.root}/app/assets/images/") do
  images = Dir['**/*.*'].inject({}) { |h,f| h.merge!(f => image_path(f)) }
end
%>

window.appData = {} unless window.appData?
window.appData.images = <%= images.to_json %>
